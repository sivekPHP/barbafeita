{% extends 'painel.html.twig' %}

{% block body %}
    <h1>Usuário</h1>

    <div class="row">
        <div class="col-md-8">

            <table class="table table-striped table-bordered">
                <tbody>
                    <tr>
                        <th>Login</th>
                        <td>{{ usuario.username }}</td>
                    </tr>
                    <tr>
                        <th>Nome</th>
                        <td>{{ usuario.nome }}</td>
                    </tr>
                    <tr>
                        <th>E-mail</th>
                        <td>{{ usuario.email }}</td>
                    </tr>
                </tbody>
            </table>

            <a class="btn btn-success pull-right" href="{{ path('usuario_edit', { 'id': usuario.id }) }}">Editar</a>
            {{ form_start(delete_form) }}
                <input type="submit" value="Deletar" class="btn btn-danger pull-left">
            {{ form_end(delete_form) }}
        </div>
    </div>
{% endblock %}
