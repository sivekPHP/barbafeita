{% extends 'painel.html.twig' %}

{% block body %}
    <h1>Novo Usuário</h1>

    <div class="row">
        <div class="col-md-8">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Salvar" class="btn btn-success pull-right" />
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
